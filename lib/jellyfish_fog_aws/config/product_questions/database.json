{
  "type": "object",
  "title": "Databases",
  "required": ["db_instance_class", "engine", "allocated_storage"],
  "properties": {
    "provisioning_answers[][db_instance_class]": {
      "title": "DB Instance Size",
      "type": "string",
      "enum": [
        "db.m3.medium",
        "db.m3.large",
        "db.m3.xlarge"
      ],
      "default": "db.m3.medium"
    },
    "provisioning_answers[][engine]": {
      "title": "DB Engine",
      "type": "string",
      "enum": [
        "aurora",
        "mysql",
        "postgresql",
        "sqlserver"
      ]
    },
    "provisioning_answers[][allocated_storage]": {
      "title": "Disk Size",
      "type": "string",
      "default": "40"
    },
    "provisioning_answers[][storage_type]": {
      "title": "Storage Type",
      "type": "string",
      "enum": [
        "ssd",
        "standard",
        "gp2",
        "io1"
      ],
      "default": "ssd"
    },
    "provisioning_answers[][availability]": {
      "title": "Storage Redundancy",
      "type": "string",
      "enum": [
        "normal",
        "reduced"
      ],
      "default": "normal"
    }
  }
}
